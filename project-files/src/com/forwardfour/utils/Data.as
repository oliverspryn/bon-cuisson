package com.forwardfour.utils {
/**
 * There are some cases when storing information that was retrieved from the server, that we
 * will need to make some dynamic properties on the object that stores the data. Use this
 * class to perform this data storage.
*/
	dynamic public class Data extends Object {
		public function Data() {
			super();
		}
	}
}